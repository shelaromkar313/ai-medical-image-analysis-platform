spring:
  datasource:
    url: jdbc:mysql://localhost:3306/medical_image_analysis_dev?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: root
    password: root
  
  jpa:
    show-sql: true
    
logging:
  level:
    com.medical.ai: DEBUG
    org.hibernate.SQL: DEBUG
